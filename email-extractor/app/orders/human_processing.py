"""#308: no message may sit SILENTLY in the terminal `human_processing` pit.

STUB (RED). The real two-layer sweep lands in the [green] commit.
"""
from __future__ import annotations


def sweep(conn, cfg, classify=None) -> int:  # pragma: no cover - stub replaced in GREEN
    return 0
